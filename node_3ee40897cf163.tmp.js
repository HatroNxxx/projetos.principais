let listaDeConvidado = ['loira', 'Alison', 'Caua', 'Vitor']
let naoConvidados = ''

for (let numeirosDeConvidado = 0; listaDeConvidado.length; numeirosDeConvidado ++) 



if (naoConvidados) {
    console.log('Não pode entra')
} else {
    console.log('Pode Entra')
}